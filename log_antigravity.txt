# FINTHIGAS — Log de Progresso (Antigravity)
> Este arquivo serve como guia de contexto para o agente Antigravity.  
> Leia-o antes de qualquer nova iteração para saber o que foi feito e o que falta fazer.

---

## 🗂️ Stack Técnica

| Item | Tecnologia |
|------|-----------|
| Framework | React 19 + Vite 8 + TypeScript 6 |
| Estilização | Vanilla CSS (`src/index.css`) com CSS variables |
| Banco de Dados | **Supabase** (PostgreSQL) |
| Cliente Supabase | `@supabase/supabase-js` v2 |
| Gráficos | `chart.js` + `react-chartjs-2` (Doughnut, Bar) |
| Ícones | `lucide-react` |
| Persistência local | `localStorage` como fallback offline |

---

## 📁 Estrutura do Projeto

```
finthigas/
├── .env.local                         # Credenciais Supabase (não comitar)
├── .env.example                       # Modelo de variáveis de ambiente
├── supabase_schema.sql                # ⚠️ Execute no SQL Editor do Supabase para criar as tabelas
├── index.html
├── src/
│   ├── App.tsx                        # Raiz + FinanceProvider + tabs de navegação
│   ├── index.css                      # Design System: dark gray/black theme, responsivo
│   ├── main.tsx
│   ├── lib/
│   │   └── supabase.ts                # Cliente Supabase configurado via VITE_*
│   ├── types/
│   │   └── index.ts                   # Tipos: Transaction, Category, Subcategory, ScheduledTransaction, etc.
│   ├── context/
│   │   └── FinanceContext.tsx         # Estado global: CRUD, sync Supabase, localStorage fallback
│   ├── utils/
│   │   ├── formatters.ts              # formatCurrency(BRL), formatDate, getMonthName
│   │   ├── defaultCategories.ts       # Categorias default (seed local)
│   │   └── mockData.ts                # Transações de demonstração
│   └── components/
│       ├── Header.tsx                 # Logo, filtros de período, badge Supabase, dropdown reset
│       ├── Navigation.tsx             # Abas (Dashboard, Entradas/Saídas, Programados, Categorias)
│       ├── SummaryCards.tsx           # KPI cards (Saldo, Entradas, Saídas, Programadas)
│       ├── Dashboard/
│       │   ├── DashboardView.tsx      # Monta todos os painéis do Dashboard
│       │   ├── ExpenseDonutChart.tsx  # Gráfico de rosca de saídas (por categoria ou subcategoria)
│       │   ├── CashFlowChart.tsx      # Gráfico de barras: Entradas vs Saídas por mês
│       │   ├── BudgetProgress.tsx     # Barras de limite de gastos por categoria
│       │   └── TopExpenses.tsx        # Top 5 maiores saídas do período
│       ├── Transactions/
│       │   └── TransactionsView.tsx   # Tabela com busca, filtro, CRUD de transações
│       ├── Scheduled/
│       │   └── ScheduledView.tsx      # Cards de lançamentos programados com efetivação 1-clique
│       ├── Categories/
│       │   └── CategoriesView.tsx     # Gerenciamento de categorias e subcategorias
│       └── Modals/
│           ├── TransactionModal.tsx   # Modal de criação/edição de transação
│           ├── ScheduledModal.tsx     # Modal de criação/edição de agendamento
│           └── CategoryModal.tsx      # Modal de criação/edição de categoria + subcategorias
```

---

## 🗄️ Banco de Dados (Supabase)

### Credenciais (`.env.local`)
```
VITE_SUPABASE_URL=https://hrqkxaakcknqzeauilyt.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_jYnKZTpuHCPE0U5wmVP3Og_Nsl6_CvA
```

### Tabelas criadas pelo `supabase_schema.sql`
1. **`categories`** — Categorias de Entrada/Saída com cor, ícone e limite de orçamento
2. **`subcategories`** — Subcategorias vinculadas via FK a `categories`
3. **`transactions`** — Lançamentos pontuais (Entradas e Saídas)
4. **`scheduled_transactions`** — Lançamentos recorrentes/programados

> ⚠️ **AÇÃO NECESSÁRIA**: Se o badge do header mostrar "Local (Offline)", significa que as tabelas **ainda não foram criadas** no Supabase.  
> Acesse o painel do Supabase → SQL Editor → cole o conteúdo de `supabase_schema.sql` → Run.

### Por que aparece "Local (Offline)"?
A detecção de conectividade foi corrigida em `FinanceContext.tsx`:
- **Antes**: `supabaseConnected = true` apenas se a tabela retornasse DADOS.
- **Depois**: `supabaseConnected = true` se a QUERY não retornar ERRO (tabelas existentes mas vazias também são válidas).

---

## ✅ O que já foi implementado

### Sessão 1 — Estrutura Base
- [x] Projeto Vite + React 19 + TypeScript 6 já estava inicializado
- [x] Dependências: `@supabase/supabase-js`, `chart.js`, `react-chartjs-2`, `lucide-react`
- [x] `.env.local` criado com credenciais do Supabase
- [x] `supabase_schema.sql` gerado com tabelas, RLS policies e seed inicial de categorias

### Sessão 1 — Componentes
- [x] `src/types/index.ts` — Interfaces TypeScript completas
- [x] `src/lib/supabase.ts` — Cliente Supabase configurado
- [x] `src/utils/formatters.ts` — formatCurrency(BRL), formatDate, getMonthName
- [x] `src/utils/defaultCategories.ts` — 7 categorias padrão com subcategorias
- [x] `src/utils/mockData.ts` — 9 transações + 4 agendamentos de demo
- [x] `src/context/FinanceContext.tsx` — Estado global + CRUD completo + sync Supabase
- [x] `src/components/Header.tsx` — Logo, filtros de período, badge status, dropdown reset
- [x] `src/components/Navigation.tsx` — Abas responsivas (ícone + label)
- [x] `src/components/SummaryCards.tsx` — 4 KPI cards responsivos
- [x] `src/components/Dashboard/DashboardView.tsx`
- [x] `src/components/Dashboard/ExpenseDonutChart.tsx` — Doughnut com toggle categoria/subcategoria
- [x] `src/components/Dashboard/CashFlowChart.tsx` — Bar chart mensal
- [x] `src/components/Dashboard/BudgetProgress.tsx` — Progress bars com alertas de limite
- [x] `src/components/Dashboard/TopExpenses.tsx` — Top 5 gastos
- [x] `src/components/Transactions/TransactionsView.tsx` — Tabela + busca + filtros
- [x] `src/components/Scheduled/ScheduledView.tsx` — Cards + efetivação 1-clique
- [x] `src/components/Categories/CategoriesView.tsx` — CRUD categorias + subcategorias
- [x] `src/components/Modals/TransactionModal.tsx`
- [x] `src/components/Modals/ScheduledModal.tsx`
- [x] `src/components/Modals/CategoryModal.tsx`
- [x] `src/App.tsx` — Raiz com FinanceProvider e roteamento por tabs

### Sessão 2 — Ajustes e Responsividade
- [x] Design System refatorado: tema cinza/preto (removido azul-marinho)
- [x] Layout totalmente responsivo (mobile-first, breakpoints 480/600/640/900px)
- [x] Nav scrollável horizontal no mobile, labels ocultos abaixo de 500px
- [x] Tabelas com scroll horizontal em mobile
- [x] Summary cards em grade 2x2 no mobile, 4 colunas no desktop
- [x] Dropdown de reset com 2 opções: "Restaurar Demonstração" e "Limpar Tudo (Blank)"
- [x] Corrigida detecção de `supabaseConnected` (via erro da query, não quantidade de dados)
- [x] `resetToBlank()` adicionado ao contexto (limpa tudo sem seed)

### Sessão 3 — Sync Cross-Device, Responsividade Mobile, Lógica Programados
> Commit: `9b0cdf2` | Data: 2026-08-13

#### 🔵 Persistência Cross-Device (`src/context/FinanceContext.tsx`)
- **Bug raiz corrigido**: o código anterior gerava IDs temporários (`tx-1234567`) e descartava o UUID real do Supabase após insert. Agora usa `.select().single()` para capturar o UUID real e substituir o estado local → IDs sempre são UUIDs do Supabase
- **Realtime subscriptions** adicionadas para `transactions` e `scheduled_transactions`: mudanças de qualquer dispositivo propagam automaticamente para os outros via `postgres_changes`
- **Carga inicial**: removida condição `if (length > 0)` — Supabase agora é sempre a fonte de verdade ao conectar, mesmo quando retorna array vazio
- Todas as funções CRUD convertidas para `useCallback` (evita re-renders desnecessários)
- ⚠️ **Para Realtime funcionar**: habilitar em Supabase Dashboard → Database → Replication → Source → marcar `transactions` e `scheduled_transactions`

#### 🟠 Responsividade Mobile
- **`src/components/Header.tsx`**: dropdown de reset usa `position: fixed` com `z-index: 500` em mobile; posição vertical calculada dinamicamente via `getBoundingClientRect()` no botão → não sobrepõe mais os cards da página
- **`src/components/Dashboard/ExpenseDonutChart.tsx`**: `ResizeObserver` detecta container < 520px; em mobile, legenda vai para **baixo** (não à direita, que causava corte de texto); texto "TOTAL GASTOS" exibido abaixo do gráfico em mobile (não mais com `left: 35%` fixo quebrado)
- **`src/index.css`**: `.reset-dropdown-menu` usa `position: fixed` + `z-index: 500` em mobile, volta para `absolute` em ≥640px; `.app-container` recebe `overflow-x: hidden`; `.app-header` recebe `position: relative; z-index: 10`; `.dashboard-grid .glass-card` recebe `min-height: 200px` em mobile

#### 🟡 Lógica da Aba "Programados" (`src/components/Scheduled/ScheduledView.tsx` + `FinanceContext.tsx`)
- **Comportamento correto ao efetivar**:
  - `once` → **deleta** o agendamento (some de Programados, aparece em Entradas/Saídas como Pago)
  - `monthly` → avança `due_date` +1 mês (continua na lista com próxima data)
  - `weekly` → avança `due_date` +7 dias
  - `yearly` → avança `due_date` +1 ano
- **View filtrada** para mostrar apenas `is_active: true`
- **Badge de status dinâmico** por vencimento: 🔴 "Vencido há Xd" / 🟡 "Vence hoje/em Xd (≤7d)" / ⚫ "Vence em Xd"
- Contador no topo: nº ativos, nº vencidos, soma das saídas a vencer
- Confirmação de efetivação informa o que vai acontecer ("será removido" ou "data será avançada")

### Sessão 4.3 — Alinhamento e Centralização Perfeita em HTML Nítido
> Data: 2026-08-13

#### 🟢 Layout Flex de 2 Colunas com Overlay Centralizado Nítido ([ExpenseDonutChart.tsx](file:///d:/00_Arquivos_de_programas/finthigas/src/components/Dashboard/ExpenseDonutChart.tsx))
- **Alinhamento e Centralização 100% Perfeitos**: O container do gráfico foi estruturado em 2 colunas flex (Container do Donut + Painel de Legenda).
- Como o canvas da rosca ocupa 100% do seu próprio subcontainer (com a legenda separada no painel ao lado), o overlay HTML **`TOTAL GASTOS: R$ XXX,XX`** fica centralizado matematicamente no ponto `(50%, 50%)` do centro exato da rosca.
- **Nitidez Vetorial em Alta Definição**: O texto é renderizado em HTML puro (Inter/Outfit 800 anti-aliased), eliminando qualquer desfoque de canvas, e as legendas ao lado exibem os valores em reais e porcentagens de cada categoria com fonte clara `#e2e8f0`.

### Sessão 6 — Melhorias: Paginação, Exportação e Notificações (Antigravity)
> Data: 2026-08-22

#### 🔵 Exportação e Paginação (TransactionsView.tsx)
- Adicionada paginação no cliente (Client-Side) limitando a exibição a 15 itens por página.
- Adicionados botões de navegação (Página Anterior e Próxima).
- Exportação de tabela de transações implementada para CSV e PDF (utilizando as libs `jspdf` e `jspdf-autotable`).

#### 🟠 Notificações de Vencimento (App.tsx)
- Inclusão de alerta visual (banner amarelo) nas abas caso existam contas programadas atrasadas ou que vencem na data de hoje.
- O banner redireciona diretamente para a aba "Programados" ao clicar em "Ver Contas".
- **Correção de Layout:** Removido o margin lateral excedente do banner para alinhar perfeitamente com os `glass-cards` do layout.
- **Notificação Nativa:** Implementada requisição de permissão e envio de notificação nativa do sistema operacional (Browser/Windows) ao entrar no app com pendências.

### Sessão 7 — Multa e Juros por Atraso em Baixas Programadas (Antigravity)
> Data: 2026-08-27

#### 🟢 Multa e Juros por Atraso (`ExecuteScheduledModal.tsx` + `FinanceContext.tsx`)
- **Detecção de Atraso**: Ao abrir o modal de baixa de um lançamento agendado com data posterior ao vencimento (`date > scheduled.due_date`), um alerta âmbar avisa a quantidade exata de dias em atraso.
- **Campos Dedicados**: Adicionados inputs para "Multa por Atraso (R$)" e "Juros / Encargos (R$)".
- **Cálculo em Tempo Real**: O modal exibe um quadro com o somatório: `(Valor Original + Multa + Juros) = Valor Total da Baixa`.
- **Efetivação Flexível**: `executeScheduledTransaction` atualizado para receber o `amount` final calculado e registrar o valor real pago no histórico e fluxo de caixa, além de incluir o detalhamento discriminado nas observações da transação.
- **Recorrências Preservadas**: Em agendamentos recorrentes (mensal, semanal, anual), o valor-base original do agendamento é mantido intacto para o próximo ciclo de vencimento.

### Sessão 8 — Arquitetura SaaS Multiusuário com Supabase Auth & RLS (Antigravity)
> Data: 2026-08-27

#### 🟣 Autenticação e Contexto (`src/context/AuthContext.tsx`) [NOVO]
- Criado `AuthProvider` com gerenciamento de sessão via `supabase.auth.onAuthStateChange`.
- Expõe: `user`, `session`, `loading`, `signIn`, `signUp`, `signOut`, `resetPassword`.
- Tradução automática das mensagens de erro da Supabase Auth para o português.

#### 🟣 Tela de Autenticação (`src/components/Auth/AuthView.tsx`) [NOVO]
- Interface premium com design Glassmorphism, dark theme e animações.
- 3 abas: **Entrar**, **Criar Conta** e **Recuperar Senha**.
- Campos com ícones, toggle de visibilidade de senha e feedback de erro/sucesso.

#### 🔵 Isolamento de Dados por Usuário
- **`supabase_schema.sql`**: Atualizado com coluna `user_id` em todas as tabelas (`categories`, `subcategories`, `transactions`, `scheduled_transactions`), políticas RLS que isolam dados por `auth.uid()` e Trigger `on_auth_user_created` que cria automaticamente as categorias padrão ao cadastrar novo usuário.
- **`src/context/FinanceContext.tsx`**: Integrado com `useAuth()`. Todos os inserts agora incluem `user_id`. Estado resetado ao deslogar. `localStorage` com namespace por `user_id` para multi-conta no mesmo device. Dados só são carregados do Supabase quando há usuário autenticado.

#### 🔵 Avatar e Logout no Header (`src/components/Header.tsx`)
- Exibe avatar com iniciais do usuário e nome/email no canto superior direito.
- Dropdown de perfil com botão "Sair da Conta".

#### 🔵 Gate de Autenticação (`src/App.tsx`)
- `AuthGate`: componente que exibe spinner de carregamento, `AuthView` (não logado) ou o app principal (`FinanceProvider > AppContent`) conforme estado da sessão.

### Sessão 9 — Autenticação Social com Google OAuth (Antigravity)
> Data: 2026-08-27

#### 🔴 Google OAuth (`src/context/AuthContext.tsx` + `src/components/Auth/AuthView.tsx`)
- Adicionada a função `signInWithGoogle()` no `AuthContext` via `supabase.auth.signInWithOAuth({ provider: 'google' })`.
- Botões oficiais estilizados **"Continuar com Google"** e **"Cadastrar com Google"** adicionados com o ícone oficial multicolorido e separador elegante nas abas de Login e Cadastro do `AuthView`.
- Redirecionamento configurado automaticamente para o `origin` da aplicação.
- Criação e vínculo automático de categorias padrão via Trigger no primeiro login do usuário via Google.

### Sessão 10 — Suporte Completo a PWA & Atalho Mobile/Desktop (Antigravity)
> Data: 2026-08-27

#### 📱 Progressive Web App (PWA)
- **Manifesto Web (`public/manifest.webmanifest`)**: Configurado com nome completo, nome curto, tema dark `#7c3aed`, modo `standalone` (sem barra de URL do navegador) e ícones responsivos.
- **Ícone Vetorial Exclusivo (`public/pwa-icon.svg`)**: Ícone em alta resolução com design de carteira financeira e gradiente roxo/índigo.
- **Service Worker (`public/sw.js` + `src/main.tsx`)**: Registro automático do Service Worker para cache inteligente de assets estáticos, suporte offline e gerenciamento de cliques em notificações.
- **Compatibilidade com iOS & Android (`index.html`)**: Meta tags para Safari iOS (`apple-mobile-web-app-capable`, `apple-touch-icon`, `theme-color`) permitindo instalação direta na tela de início de iPhones e Androids como aplicativo nativo.

---

## 🔴 O que AINDA PRECISA ser feito / Pendências

- [ ] **Habilitar Google OAuth no Supabase**: Configurar Client ID e Client Secret em Supabase → Authentication → Providers → Google.
- [ ] **Habilitar Realtime no Supabase** para `transactions` e `scheduled_transactions` (Dashboard → Database → Replication).
- [ ] ⚠️ **IMPORTANTE**: Executar o `supabase_schema.sql` no SQL Editor do Supabase para ativar o RLS e o Trigger de criação de categorias.

---

## 🧪 Como Rodar Localmente

```bash
cd d:\00_Arquivos_de_programas\finthigas
npm run dev
```

## 🚀 Como Fazer Deploy

```bash
npm run build
# A pasta dist/ gerada pode ser publicada em Vercel, Netlify, etc.
# O Vercel detecta o push no GitHub e faz deploy automaticamente.
```

---

## 🔑 Variáveis de Ambiente

| Variável | Valor |
|----------|-------|
| `VITE_SUPABASE_URL` | `https://hrqkxaakcknqzeauilyt.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | `sb_publishable_jYnKZTpuHCPE0U5wmVP3Og_Nsl6_CvA` |

---

_Última atualização: 2026-08-27 — Sessão 10 (Suporte Completo a PWA & Atalho Mobile/Desktop)_


